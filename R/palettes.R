#' Commonly used palettes
#'
#' These include a colorblind friendly palette set in addition to a
#' set of palettes based on the brand identities of all 14 Big Ten
#' schools (as of April 2024).
#'
#' @rdname guzioR
#' @format ## `guzior.pal`
#' An object of class `palettes_palette` with
#' `r length(guzior.pal)` color palettes.
#' Use `names(guzior.pal)` to return all palette names.
"guzior.pal"

#' @rdname guzioR
#' @format ## `b10.pal`
#' An object of class `palettes_palette` with
#' `r length(b10.pal)` color palettes.
#' Use `names(b10.pal)` to return all B10 palette names.
"b10.pal"

