#' Commonly used palettes

#' @rdname guzioR
#' @format ## `b10.pal`
#' An object of class `palettes_palette` with
#' `r length(b10.pal)` discrete colour palettes.
#'  Use `names(b10.pal)` to return all palette names.
"b10.pal"

#' @rdname guzioR
#' @format ## `guzior.pal`
#' An object of class `palettes_palette` with
#' `r length(guzior.pal)` sequential colour palettes.
#'  Use `names(guzior.pal)` to return all palette names.
"guzior.pal"
